# Boundaries

See `docs/BOUNDARIES.md` for ownership boundaries.

Provider adapters must not own architectural truth.

Generated state belongs in `.sixeyes/`; source contracts belong in `contracts/`.
