# Constraints

- Prefer deterministic project evidence over broad inference.
- Do not introduce provider-specific logic into `core/`.
- Do not cross domain boundaries without reading dependency evidence.
- Keep generated code consistent with the detected architecture.
