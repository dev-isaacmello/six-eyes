# AI

Six Eyes is an AI-native architectural middleware layer. Agents should load
`docs/AI.md`, select a protocol from `protocols/`, select a skill from
`skills/`, and ground work in generated `.sixeyes/` maps before large
implementation, review, refactor, debugging, or migration work.
