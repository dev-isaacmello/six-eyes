# Cursor Templates

Cursor templates should generate rules that are:

- stable across sessions
- scoped to project maps
- strict about boundary regressions
- compact enough to coexist with local project rules
