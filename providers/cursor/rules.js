export const rules = [
  "Keep generated edits inside the ranked context unless the task requires expansion.",
  "Do not cross domain boundaries without explicit evidence from the dependency graph.",
  "Use architecture memory as the source of truth for layer intent.",
];
