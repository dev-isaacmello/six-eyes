# Codex Adapter

Use Six Eyes maps to shape coding constraints:

- `architecture.json` for layer boundaries
- `dependency-graph.json` for coupling risk
- `context-rankings.json` for token allocation

When uncertain, prioritize low-entropy changes over large rewrites.
